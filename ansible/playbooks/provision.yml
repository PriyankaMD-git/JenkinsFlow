---
- name: Provision base environment
  hosts: localhost
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install required packages
      apt:
        name:
          - curl
          - git
          - docker.io
          - nodejs
          - npm
        state: present

    - name: Enable and start Docker
      service:
        name: docker
        enabled: yes
        state: started

    - name: Create app directory
      file:
        path: /opt/jenkinsflow
        state: directory
        owner: "priyanka"
        mode: "0755"