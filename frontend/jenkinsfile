pipeline {
  agent any

  environment {
    DOCKER_IMAGE = 'quiz-frontend'
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Install Dependencies') {
      steps {
        sh 'npm install'
      }
    }

    stage('Build React App') {
      steps {
        sh 'npm run build'
      }
    }

    stage('Run Tests') {
      steps {
        sh 'npm test || echo "No tests defined"'
      }
    }

    stage('Build Docker Image') {
      steps {
        sh 'docker build -t $DOCKER_IMAGE .'
      }
    }

    stage('Push Image (Optional)') {
      when {
        expression { return false } // Set to true if pushing to registry
      }
      steps {
        echo 'Push step placeholder'
      }
    }
  }
}